-- Test data for StoreControllerIntegrationTest - Register Store Tests
-- Using fixed UUIDs for predictable testing

-- No store data needed for register tests, but Spring @Sql requires at least one SQL statement
SELECT 1;
